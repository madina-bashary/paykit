# The demo imports React types through the workspace package; hoist them so
# Next's compiler resolves a single copy of @types/react.
public-hoist-pattern[]=@types/react*
strict-peer-dependencies=false
